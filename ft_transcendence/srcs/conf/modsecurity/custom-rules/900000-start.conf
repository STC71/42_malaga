# This rule acts as a marker indicating the start of custom rules loading. It does not affect request processing.
SecAction "id:900000,phase:1,pass,nolog,t:none,msg:'Custom rules loaded (start marker)'"
